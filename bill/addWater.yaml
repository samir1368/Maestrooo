appId: ${APP_ID}
---

- tapOn: 
   id: "main"
- tapOn: 
   id: "edtBillId"
- inputText: ${WATER_BILL_ID}
- tapOn: 
   id: "save"
- runFlow:
    when:
      visible: "نام قبض"
    file: saveBill.yaml

- runFlow:
    when:
       visible: "قبض آب"
    commands: 
        - back
- runFlow:
    when:
       visible: "قبض آب"
    commands: 
        - back
